/// <reference types="vite/client" />
//
// Declares `import.meta.env` (BASE_URL, MODE, DEV, PROD) to TypeScript. Vite
// scaffolds this file by default; it was absent here, so any use of
// `import.meta.env` failed to typecheck. BASE_URL is what makes asset URLs
// survive being served from a subpath rather than the domain root.
